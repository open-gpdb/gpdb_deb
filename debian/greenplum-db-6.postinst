#!/bin/bash

set -e

GPADMIN=gpadmin
GPVER=_GPVER_
GPHOME=/opt/greenplum-db

if [ "$1" = configure ]; then

  chown -R ${GPADMIN}:${GPADMIN} ${GPHOME}-${GPVER}

  cat << _EOF_ > /etc/ld.so.conf.d/gpdb.conf
${GPHOME}-${GPVER}/lib/
_EOF_

  /sbin/ldconfig
fi
